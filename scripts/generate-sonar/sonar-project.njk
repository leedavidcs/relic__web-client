# must be unique in a given SonarQube instance
sonar.projectKey={{ pkg.name }}

# this is the name and version displayed in the SonarQube UI. Was mandatory prior to SonarQube 6.1.
sonar.projectName={{ pkg.name }}
sonar.projectVersion={{ pkg.version }}
sonar.projectDescription=Starter for a frontend application
 
# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# This property is optional if sonar.modules is set. 
sonar.sources=.
sonar.tests=.
sonar.test.inclusions={{ inclusions }}
sonar.exclusions={{ exclusions }}

sonar.testExecutionReportPaths=test-reports/sonar.xml

sonar.typescript.lcov.reportPaths=coverage/lcov.info

sonar.links.scm={{ pkg.repository.url }}
